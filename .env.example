# Local development only. Copy to .env; never commit real values.
DATABASE_URL=postgresql://roxy:roxy_dev_only@localhost:5432/roxy
API_HOST=127.0.0.1
API_PORT=4100
LOG_LEVEL=info

# Add only when the AI phase begins.
AI_PROVIDER=disabled
GEMINI_API_KEY=
EMBEDDING_MODEL=gemini-embedding-2
EMBEDDING_DIMENSION=768

