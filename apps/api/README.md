# Roxy API

Reserved for the TypeScript/Fastify API. Initialize it during Phase 1 rather than installing speculative dependencies now.

Planned responsibilities: device authentication, validated idempotent ingestion, timeline/facts endpoints, scheduled processing, and chat orchestration.

