# Architecture Decisions

Decision records explain choices that would otherwise be repeatedly reconsidered. Add one for changes to platform, persistence, sync, authentication, AI provider boundary, collection method, or privacy defaults.

Status values: `proposed`, `accepted`, `superseded`, `rejected`.

Use the next four-digit number and the structure of the existing records.

