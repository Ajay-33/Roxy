# T-0407: Notification metadata contract and authenticated server persistence

- Status: done
- Phase: 4.2 / Automatic collection and owner dashboard integration
- Estimate: 1–4 hours
- Depends on: T-0406

## Goal

Add a metadata-only notification event contract and authenticated idempotent hosted persistence, with no text payload or new cloud secret.

## Read first

- `AGENTS.md`
- `tasks/done/T-0406.md`
- `docs/PRIVACY-SECURITY.md`
- `packages/contracts/src/event-envelope.ts`
- `apps/api/src/app.ts`
- `packages/database/src/event-store.ts`
- `packages/database/migrations/`

## In scope

- Define validated `notification.posted` and `notification.removed` metadata payloads containing only package identifier, local lifecycle digest, and redaction count where applicable.
- Ensure the API accepts them through the existing authenticated/idempotent event envelope path.
- Add deterministic server notification timeline/count query support for the paired device.
- Add contract/API/database tests using synthetic data only.

## Out of scope

- Notification text upload, dashboard UI, Android queue changes, cloud credential changes, or deployment before verification.

## Acceptance checks

- [x] Automated: valid metadata-only notification events are accepted idempotently; text/unsafe fields are rejected.
- [x] Automated: authenticated notification query returns deterministic counts/timeline without payload text.
- [x] Documentation: data boundary and no-text/no-new-secret rule recorded.

## Battery note

- Expected callbacks/queries per day: no phone behavior change in this server task.
- Expected rows/upload bytes per day: server-ready only; Android upload remains T-0408.
- Location/foreground service/wake lock impact: none.

## Privacy note

- Data classes touched: private metadata only—package identifier, lifecycle digest, event time, timezone, kind, and redaction count.
- Redaction/retention impact: no text accepted or stored; existing notification text retention does not begin.
- New permission or secret: none.

## Verification record

- Automated checks: `pnpm check` and `git diff --check` passed on 2026-08-22.
- Manual checks: deployed hosted API `/health` returned 200; the authenticated notification-summary endpoint returned 401 without a credential, confirming it is live and protected.
- Not run and why: end-to-end metadata upload is T-0408.

## Handoff

- Outcome: hosted Roxy accepts only validated metadata-only notification envelopes and exposes a protected summary endpoint.
- Files changed: shared contract, API query endpoint/tests, database notification query, privacy/task tracking.
- Risks/follow-ups: T-0408 adds Android queueing/sync after this contract is verified.
- Suggested next task ID: T-0408.
