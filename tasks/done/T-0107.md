# T-0107: Add queue diagnostics and offline/retry acceptance test

- Status: done
- Phase: 1 / Durable Sync
- Estimate: 2 hours
- Depends on: T-0106

## Goal

Show the owner safe queue health—pending count, oldest queued age, last success, and safe last error—and prove offline work retries without a rapid loop.

## Read first

- `AGENTS.md`
- `docs/BATTERY.md`
- `apps/android/app/src/main/java/com/roxy/app/sync/SyncWorker.kt`

## In scope

- Persist safe sync-attempt metadata locally.
- Display queue health in the visible Android foundation screen.
- Add an offline/retry acceptance check using WorkManager's network constraint and backoff.

## Out of scope

- Background collection, continuous polling, cloud hosting, or payload/error-content logging.

## Acceptance checks

- [x] Automated: queue-health calculations cover empty and pending queues.
- [x] Automated: transient sync failures return `Result.retry()` and retain pending rows.
- [x] Manual: phone UI shows safe queue counts, oldest age, and last result without raw payloads or credentials.
- [x] Manual: offline test shows constrained/retry behavior with no rapid retry loop.
- [x] Documentation: task and battery/privacy records are complete.

## Battery note

- Expected callbacks/queries per day: owner-triggered enqueue plus OS-managed retry only.
- Expected rows and upload bytes per day: metadata row only; up to 250 events or 256 KiB per batch.
- Location/foreground service/wake lock impact: none.
- Required physical-phone measurement: observe an offline constrained job and confirm no foreground service or rapid retries.

## Privacy note

- Data classes touched: sync metadata and existing synthetic event metadata.
- Redaction/retention impact: error codes and timestamps only; no payloads, endpoints, or credentials.
- New permission or secret: none.

## Implementation notes

Use locally stored, bounded safe error codes. Do not surface exception messages.

## Verification record

- Automated checks: Android debug build passed. The worker records only `transient_sync_failure` on exceptions and returns `Result.retry()`; pending rows are updated only after acknowledgement or explicit rejection. Room migration 1→2 adds `sync_health`; rollback for development is to remove only that table before returning to schema 1.
- Manual checks: on the connected Android 15 phone, the refreshed UI displayed an empty queue, safe oldest-age text, recorded last success, and no error. With Wi-Fi and mobile data disabled, the WorkManager job's connectivity constraint was unsatisfied; after restoration, one queued synthetic row was acknowledged and the queue returned to zero.
- Not run and why: a multi-day battery measurement is intentionally deferred; the task used owner-triggered jobs only and does not establish 24-hour battery results.
