# T-0408: Android notification queue and WorkManager sync

- Status: done
- Phase: 4.2 / Automatic collection and owner dashboard integration
- Depends on: T-0407

## Goal

Automatically queue policy-approved metadata-only notification events into the existing local event outbox and schedule bounded WorkManager sync, with no manual operational action.

## Read first

- `AGENTS.md`
- `tasks/done/T-0407.md`
- `docs/BATTERY.md`
- `docs/PRIVACY-SECURITY.md`
- `apps/android/app/src/main/java/com/roxy/app/notifications/`
- `apps/android/app/src/main/java/com/roxy/app/sync/`
- `apps/android/app/src/main/java/com/roxy/app/data/`

## In scope

- Convert approved metadata events into validated local outbox envelopes with no text payload.
- Schedule existing constrained WorkManager sync after a bounded local insert; no listener network call.
- Keep text/redaction rows local-only and outside the outbox.
- Add deterministic unit tests for envelope generation, queue gating, and no-text payload.

## Out of scope

- Dashboard rendering, deployment, text upload, manual queue/sync UI, or adding a foreground service/wake lock.

## Acceptance checks

- [x] Automated: approved metadata produces one valid local pending event; blocked/text content does not enter payload.
- [x] Automated: listener schedules sync but never opens a network connection directly; Android tests/build pass.
- [x] Manual: phone synthetic event enters and leaves the outbox through the hosted API without inspecting content.
- [x] Battery/privacy: no polling, no direct collector network, no text upload.

## Battery note

- Expected callbacks/queries per day: notification callbacks only; sync is existing constrained WorkManager batching.
- Expected rows/upload bytes per day: one metadata envelope per deduplicated approved lifecycle event; small JSON payload, bounded by existing 250-event/256 KiB batch.
- Location/foreground service/wake lock impact: none.

## Privacy note

- Data classes touched: metadata package/digest/kind/time/timezone/redaction count only.
- Redaction/retention impact: text remains local-only and seven-day retained; never enters outbox.
- New permission or secret: none.

## Verification record

- Automated checks: `testDebugUnitTest`, `assembleDebug`, and `git diff --check` passed on 2026-08-22. Exporter tests prove the queued envelope is pending/private and includes only package identifier, lifecycle digest, and redaction count; existing gate tests prove disabled, blocked, missing-key, and group-summary events fail closed.
- Manual phone check: a policy-approved platform synthetic notification was observed by the listener. The debug-only status surface showed one metadata row, then zero pending events and one acknowledged notification event after constrained WorkManager reached the hosted API. No content was inspected.
- Privacy/battery: callback-driven only; listener never opens a network connection, has no wake lock or foreground service, and queues only the first deduplicated metadata event. Text remains local-only with seven-day retention.

## Handoff

- Outcome: approved notification metadata now automatically travels phone → local outbox → constrained WorkManager → authenticated hosted API/DB, with acknowledgement visible through the debug verification surface.
- Files changed: notification exporter/listener data boundary, privacy documentation, task tracking, and deterministic exporter tests.
- Risks/follow-ups: owner-facing dashboard rendering is T-0409; text upload remains deliberately out of scope and disabled.
- Suggested next task ID: T-0409.
