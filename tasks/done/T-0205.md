# T-0205: Server daily app totals and seeded query tests

- Status: done
- Phase: 2 / App usage
- Depends on: T-0204

## Goal

Expose authenticated, aggregate-only daily app totals with seeded API tests.

## Acceptance checks

- [x] Automated: repository typecheck and existing tests pass.
- [x] Automated: seeded daily-total route tests include ordering, date bounds, and authentication.
- [x] Documentation: API contract, data handling, and task record complete.
- [x] Manual: aggregate-only Android bucket export, authenticated sync, PostgreSQL persistence, and daily-total query verified.

## Privacy note

- Data classes touched: aggregate bucket package identifier and duration only.
- No raw UsageEvents, labels, or timestamps beyond the aggregate bucket are returned.

## Verification record

- Automated checks: `pnpm check`, including the seeded authenticated totals route test, passed on 2026-08-20. Android `:app:testDebugUnitTest` and `:app:assembleDebug` passed on 2026-08-20, including aggregate-payload and export-idempotency coverage.
- Manual checks: on a connected physical phone, Usage Access was enabled and an owner-triggered read observed 115 events, storing 63 new local observations (3,090 total). Local aggregation persisted 318 15-minute buckets. The paired local API acknowledged the first 250 aggregate events and a second batch of 68 with zero rejections; PostgreSQL then contained exactly 318 `usage.bucket` rows. The authenticated daily endpoint returned a 200 response for the matching Asia/Calcutta calendar-day bounds, with five totals, a deterministic duration sum of 1,127,410 ms, and verified ordering; the unauthenticated request returned 401. No raw UsageEvent payloads were logged or uploaded.
- Scope note: the endpoint accepts aggregate-only `usage.bucket` events and performs deterministic date-bounded totals; it does not receive raw UsageEvents. Android now uses the paired installation's generated stable device ID and current observed timezone for newly queued aggregate events, constructs payload JSON with `JSONObject`, derives export eligibility from the local event queue, and schedules a follow-up batch when more than 250 events remain.
