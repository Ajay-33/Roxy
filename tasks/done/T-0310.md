# T-0310: Android Today dashboard with local app labels and aggregate cards

- Status: done
- Phase: 3.1 / Owner-facing UI
- Estimate: 1–4 hours
- Depends on: T-0307

## Goal

Turn the authenticated aggregate summary into a calm Today dashboard with exact totals, locally resolved app labels, and clear completeness/evidence state.

## Read first

- `AGENTS.md`
- `docs/DESIGN.md`
- `apps/android/app/src/main/java/com/roxy/app/MainActivity.kt`
- `apps/android/app/src/main/java/com/roxy/app/timeline/TimelineReader.kt`
- `apps/api/README.md`

## In scope

- User-initiated summary read with total duration and top aggregate app identifiers.
- Resolve app labels only on the device; never log or persist them remotely.
- Compose Today cards and accessible incomplete-data/evidence wording.
- Parser and local label-resolution unit tests.

## Out of scope

- New collection, permissions, AI, raw event display, background reads, or web dashboard.

## Acceptance checks

- [x] Exact total and top app rows render after an authenticated read.
- [x] App labels are resolved locally and missing packages have a safe fallback.
- [x] Incomplete/evidence state is visible; no sleep claim.
- [x] Android tests/build and physical-phone check pass.

## Battery note

- User-initiated reads only; no background change.

## Privacy note

- Owner-visible labels remain on device and are excluded from logs, fixtures, commits, and AI prompts.

## Verification record

- Automated checks: `gradlew.bat testDebugUnitTest assembleDebug --console=plain` passed, including focused summary parser and fallback-label tests.
- Manual checks: completed on the owner-authorized USB-debugging phone. Installed the debug APK, refreshed Today against the paired hosted API, and confirmed the aggregate success state plus the total/top-app/evidence cards rendered. No personal totals, labels, identifiers, payloads, or credentials were inspected or recorded.
- Not run and why: a multi-day battery comparison is not applicable; this task adds only a user-initiated authenticated read and UI rendering.

## Handoff

- Outcome: Today now displays authenticated aggregate activity, locally resolved owner-visible labels, a non-identifying unavailable-app fallback, and conservative evidence/completeness wording.
- Files changed: `MainActivity.kt`, `UsageSummaryReader.kt`, `UsageSummaryReaderTest.kt`, `docs/BATTERY.md`, `docs/PRIVACY-SECURITY.md`, task tracking.
- Risks/follow-ups: the existing aggregate collector still needs its separately deferred multi-day accuracy/battery validation; this UI task makes no new collection claim.
- Suggested next task ID: T-0311.
