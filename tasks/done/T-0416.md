# T-0416: Explicit notification metadata controls and real-package qualification

- Status: done
- Phase: 4.2 / Notification consent correction
- Depends on: T-0415

## Goal

Make metadata-only notification collection default-on after Android access is granted, with flexible local block/restrict rules and a real-package automatic callback/sync/dashboard qualification.

## In scope

- Restore clear Settings controls for local package block/restrict rules.
- Verify real notification metadata through the automatic callback/sync/dashboard path without reading notification content.
- Keep metadata-only as the default and text excluded.

## Out of scope

- Notification text, installed-app enumeration, or any new permission.

## Acceptance checks

- [x] Automated: default metadata policy and local block rules are valid; Android tests/build pass.
- [x] Manual: default-policy callback produces metadata count/timeline through automatic path, without content inspection.
- [x] Privacy: notification text remains excluded; owner can pause the collector and local policies remain available for restriction.

## Verification record

- Automated checks: `testDebugUnitTest`, `assembleDebug`, and `git diff --check` passed on 2026-08-22.
- Manual checks: installed build on paired phone. With no per-package rule, a safe synthetic callback changed local metadata count from `0` to `1`, acknowledged hosted count from `2` to `3`, and protected summary count from `2` to `3`. The user subsequently confirmed WhatsApp metadata also works. No personal notification content was inspected.
- Privacy/battery: metadata-only callback path; no text/action/image data, polling, foreground service, wake lock, or direct collector networking.

## Handoff

- Outcome: metadata collection defaults on after Android Notification Access, with local rules available to restrict packages; the owner no longer needs package IDs to receive default metadata.
- Files changed: notification policy/default store, Settings wording, unit tests, privacy documentation, and task tracking.
- Risks/follow-ups: T-0417 turns collected metadata into deterministic owner analytics.
- Suggested next task ID: T-0417.
