# T-0312: Android app-label fallback and Today-card polish

- Status: done
- Phase: 3.1 / Owner-facing UI
- Estimate: 1–4 hours
- Depends on: T-0311

## Goal

Show locally resolvable installed app labels in Today, while presenting an exact local identifier instead of a vague unavailable-app message when an older aggregate references an app that is no longer installed.

## Read first

- `AGENTS.md`
- `docs/DESIGN.md`
- `apps/android/app/src/main/java/com/roxy/app/timeline/UsageSummaryReader.kt`
- `apps/android/app/src/main/java/com/roxy/app/MainActivity.kt`

## In scope

- Local installed-app label resolution and an owner-visible exact identifier fallback.
- Today row hierarchy/polish that makes the fallback explanatory rather than alarming.
- Focused unit tests and physical-phone UI verification without recording personal app data.

## Out of scope

- New collection, permissions, backend payload changes, logging of usage data, cloud changes, or exposing credentials.

## Acceptance checks

- [x] Installed app identifiers resolve to their local display labels.
- [x] A missing local app label has an exact, non-vague identifier fallback.
- [x] Today rows retain explicit aggregate/evidence/incomplete wording.
- [x] Android tests/build and a physical-phone UI check pass.

## Battery note

- User-initiated summary read only; no background work, new queries, or network behavior.

## Privacy note

- Local owner UI may show app labels and aggregate timings. Development records continue to exclude personal usage data and always exclude credentials/secrets.

## Verification record

- Automated checks: `gradlew.bat testDebugUnitTest assembleDebug --console=plain` and `pnpm check` passed.
- Manual checks: installed the debug APK on the owner-authorized USB-debugging phone, refreshed Today against the existing paired service, and confirmed the aggregate refresh state. Detailed labels and timings were not extracted into development records.
- Not run and why: forced missing-package testing is covered by the deterministic local unit test; it would require changing real personal phone data to reproduce physically.

## Handoff

- Outcome: Today uses Android's local installed-app label when available; otherwise it presents the exact owner-visible identifier with a clear local-label explanation.
- Files changed: Android summary-label model/UI/tests, privacy and battery notes, task tracking.
- Risks/follow-ups: a stale aggregate can only show an identifier if the corresponding app is no longer installed; no remote lookup is attempted.
- Suggested next task ID: T-0401.
