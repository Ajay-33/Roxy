# T-0207: Hosted validation environment and migration runner

- Status: done
- Phase: 2 / App usage
- Depends on: T-0205

## Goal

Provide the minimal hosted environment required to run T-0206 without a laptop, USB cable, or `adb reverse`: an HTTPS API, managed PostgreSQL, repeatable schema migration, and verified aggregate-only phone sync.

## Approved deployment decisions

- API: Render Free web service.
- Database: Supabase Free PostgreSQL, in the closest compatible shared region to the API.
- Availability: an external 10-minute probe calls a public, payload-free `/health` endpoint to avoid Render's 15-minute idle spin-down during the three-day validation.
- Access: public HTTPS endpoint protected by a fresh hosted-validation bearer credential.
- Budget: free tiers only; services and validation data are removed after T-0206.

## Scope

- Deploy the existing API as a single Render Free HTTPS service; do not add cloud collectors, analytics, AI services, or third-party data processors beyond the approved health probe.
- Provision a managed PostgreSQL database and keep its credentials in provider-managed secret storage.
- Add a repeatable migration runner that records applied migration IDs and applies `packages/database/migrations/0001_events.up.sql` before the API serves traffic.
- Document a migration preflight, backup/snapshot step, verification query, and rollback/recovery notes. Do not run destructive down migrations against a database containing validation data.
- Configure the API only with the hosted database URL and a new 32+-character device credential held in secret storage.
- Use Supabase's SSL-enforced IPv4 shared session pooler for the Render service; do not expose database credentials to the phone.
- Pair the phone to the HTTPS endpoint, verify offline retry and idempotent replay with aggregate-only `usage.bucket` events, and record only counts and safe error codes.

## Acceptance checks

- [x] Automated: migration runner has tests for a fresh database and an already-migrated database.
- [x] Automated: API starts only after required migrations have succeeded.
- [x] Manual: HTTPS health/API request succeeds with the hosted credential; unauthenticated request is denied.
- [x] Manual: phone pairs with the hosted endpoint and syncs aggregate-only buckets without USB or `adb reverse`.
- [x] Manual: replay does not add duplicate `usage.bucket` rows; daily totals match the stored aggregate rows.
- [x] Documentation: provider, region, monthly spend limit, secret locations, migration procedure, recovery procedure, and service teardown steps are recorded without secrets.
- [x] Privacy: no raw UsageEvents, app labels, credentials, database URLs containing passwords, or production payloads appear in logs, fixtures, screenshots, or task records.

## Hosted validation record

- API: Render Free HTTPS service, `roxy-validation-api`; endpoint configuration is stored in the phone's encrypted pairing record.
- Database: Supabase Free PostgreSQL, Tokyo (`ap-northeast-1`) shared IPv4 session pooler. Render holds the database URL and bearer credential as masked environment secrets; the phone receives only its bearer credential.
- Availability: user-configured cron-job.org request every 10 minutes to the payload-free `/health` endpoint. It does not contact the phone or database.
- Spend limit: free tiers only; review provider dashboards before any upgrade or paid add-on. Tear down the Render service, cron job, and Supabase project/data when T-0206 completes.
- Migration procedure: the API applies numbered `*.up.sql` migrations transactionally at startup and records IDs in `schema_migrations`. Before a material migration, take the managed-database backup/snapshot available to the provider; verify `schema_migrations` and the expected table/query afterward. Do not apply destructive down migrations to validation data.
- Recovery: stop the Render service and revoke/replace the hosted device credential if a hosted sync path is compromised; keep the phone queue local for retry. Restore from the pre-migration provider snapshot only when necessary.
- Phone proof (2026-08-20): a clean app state collected 2,921 preceding-24-hour observations and formed 312 local 15-minute aggregate buckets. It queued and acknowledged 312 `usage.bucket` events to the hosted service. Supabase contained 312 rows and 33,361,825 aggregate milliseconds. The authenticated daily endpoint returned 200 with 46 aggregate package totals summing to the same duration; the unauthenticated request returned 401. Immediate replay queued 0 buckets and left the phone queue empty.

## Battery note

- This task may use the existing WorkManager network-constrained sync only. It must not add polling, a foreground service, or a wake lock.
- The external 10-minute HTTP health probe does not contact the phone or database and has no phone battery impact.
- Before T-0206, record expected sync wake frequency, bytes/day, and rows/day for the chosen validation schedule.

## Rollback notes

- If deployment or migration verification fails, stop the hosted API, revoke the hosted device credential, and keep the phone queue local for retry.
- Restore the managed database from its pre-migration snapshot if required; do not apply destructive down migrations to validation data.

## Handoff

- T-0206 remains blocked until every hosted prerequisite passes. Its Day 1 starts only after the phone has been paired to the hosted HTTPS endpoint.
